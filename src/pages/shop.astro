---
import Layout from "../layouts/Layout.astro"
import Top from "../components/top.astro"
import beers from "../data" 
import ProductCard from "../components/productCard.astro"

console.log(beers)
---

<Layout title="tienda">
  <div class="w-full max-w-3xl backdrop-blur-sm text-gray-50">
  <Top/>
	<main class="w-full flex justify-center w-full flex-col gap-8 p-10">
    <div class="w-full flex justify-center w-full flex-col gap-2">
      <p class="w-full text-center text-xl font-medium">CONOCE NUESTRAS CERVEZAS</p>
      <p class="w-full text-center text-base">A un par de clicks de distancia</p>
    </div>

    <ul class="flex items-center flex-col sm:flex-row sm:flex-wrap justify-center w-full gap-8 sm:py-10 py-5">
{
  beers.map((beer) => {
      return  (

        <li class="w-full flex justify-center sm:w-1/3 sm:hover:w-2/5 duration-200 transition-all">
          <a href="#">
            <ProductCard product={beer} />
          </a>
        </li>
      )
  })
}
    </ul>
    <div class="w-full flex justify-center">
      <button class="flex align-center justify-center gap-2 bg-amber-500 text-black w-full sm:w-1/3 py-3 block text-center hover:bg-neutral-900 hover:text-amber-500 transition-all duration-200 hover:border-amber-500 hover:border">
        Ir al carrito
      </button>
    </div>
	</main>
  </div>
</Layout>
